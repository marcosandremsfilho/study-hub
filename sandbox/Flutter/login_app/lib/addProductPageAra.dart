import 'package:flutter/material.dart';

class AddProductPageArea extends StatefulWidget {
  @override
  addProductPageArea createState() => addProductPageArea();
}

class addProductPageArea extends State<AddProductPageArea> {
  int selectedIndex = 0;
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Container(
      decoration: const BoxDecoration(color: Colors.blue),
    ));
  }
}
